package com.agriguardian.enums;

/**
 * type of zone/anchor
 * if a zone is 'KEEP_IN' an object should stay inside of it
 * if a zone is 'KEEP_OUT' an object should stay away of it
 */
public enum ZoneRule {
    KEEP_IN,
    KEEP_OUT;

}
